_PRINTF
NAME 
        _printf

SYNOPSIS
        #include "main.h"
        #include <stdarg.h>

        int _printf(const char *const format, ...)

DESCRIPTION
        The function in the _printf() give an outpout according to a format, the function write the output with a string format 
        with the specifies of '%' 

Conversion specifiers
        A charater specifies the type of conversion of the argument
    d, i allow an output format of an int argument in decimal
    o  allows an output format of an int argument in octal
    x  allows an output format of an int argument in hexadecimal and use "abcdef"
    c  allows an output format of a char in a character
    s  allows an output format of a char in a string

RETURN VALUE
        in case of success the function return the number of characters printed

EXAMPLE
        To print a sentence :
            #include "main."
            #include <stdarg.h>
            _printf("String:[%s]\n", "I am a string !")

        To print an integer :
            #include "main."
            #include <stdarg.h>
            _printf("Negative:[%d]\n", -762534)


                                            2022-03-16
                                    Mustapha Abourar - Lucile Deleforge
