#include "main.h"
#include <stdlib.h>
#include <stdarg.h>

/**
 * _char - print's a character.
 * @c: charactere to print.
 * 
 * Return: always 1.
 */
int _char(va_list.choice)
{
    char ch = (char)va_arg(c, int)

    _putchar(ch);
    return(1);
}